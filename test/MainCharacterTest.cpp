//
// Created by Lorenzo Nuti and Paolo Valcepina on 02/04/17.
//

#include <SFML/Graphics.hpp>

#include "gtest/gtest.h"
//#include "../MainCharacter.h"

TEST(MainCharacter, Constructor) {
    sf::RenderWindow window(sf::VideoMode::getDesktopMode(), "Catula");
    //MainCharacter *c = MainCharacter::getInstance();
    //ASSERT_FLOAT_EQ(200, c->getPosition().x);
    //ASSERT_FLOAT_EQ(200.1, c->getPosition().y);
}
